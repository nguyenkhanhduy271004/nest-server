services:
  mongodb:
    image: mongo:5.0.15
    environment:
      MONGO_INITDB_ROOT_USERNAME: nguyenduy271004
      MONGO_INITDB_ROOT_PASSWORD: kuy271004
    ports:
      - 27017:27017
    volumes:
      - mongodb_data_container:/data/db

volumes:
  mongodb_data_container:

    # docker compose -p hoidanit-mongodb-4rn up -d
